# canary/

CI self-test files containing **intentional violations**. The MISRA CI
job fails if it does not flag these files — proving the checker is
actually running. Not part of Canon-C. Do not include, fix, or
lint-clean anything in this directory.
